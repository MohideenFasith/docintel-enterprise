# Security

- Do not commit secrets. Use environment variables for credentials.
- The sample services are intentionally self-contained and require explicit integration work before production deployment.
- Authentication/authorization boundaries are represented in the architecture but should be connected to your organization identity provider before exposing the service publicly.
- Report dependency or application vulnerabilities privately to the repository owner.
